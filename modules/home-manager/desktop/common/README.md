# Common desktop configuration modules

This directory contains modules that enable packages and configurations that are
not tied to a specific window manager or desktop environment (e.g. Hyprland,
Sway, etc).
